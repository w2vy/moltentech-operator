# Dependencies
node_modules/
**/node_modules/

# Env / secrets — never commit real keys
.env
.env.*
!.env.operator.example

# Operator instance data (signed manifest + signing key) — never commit
flux-app/manifest.json
manifest-key.pem
manifest-pubkey.txt
manifest.body.json

# Build output
dist/
*.tsbuildinfo

# Python / arcane-mage local artifacts
__pycache__/
*.pyc
.venv/
